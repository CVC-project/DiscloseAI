# 다음 세션 프롬프트 — Wave 2 신규 산업 T1 골든 8본 (브랜치 `feat/golden-wave2-industries`)

> 이 파일은 다음 세션에 붙여넣을 핸드오프다. 갱신: 2026-08-05.

## 현재 상태

- **골든 20본 목표 중 17본 완주**: 삼성 005930(T0) + T1 16사. 전원 `--strict` §1~§16 갭 0.
- **Wave 2 진행 5/8 완주** — 철강 현대제철 004020(V-102) · 정유 S-Oil 010950(V-104) · 식품 CJ제일제당 097950(V-105) ·
  유통 이마트 139480(V-107) · **항공 대한항공 003490(V-109, 08-04)**.
  남은 3본: **게임 크래프톤 259960** · 엔터 하이브 352820 · 유틸리티 한국전력 015760.
  **PR 없이 본당 1커밋 push만**(리더 지시). T2 확장은 이 브랜치 범위 밖.
- **대한항공 003490 완주 요약**: 매출 [17.87→25.23조]인데 영업이익 [2.11→1.11조](5년 최저) — 성장이 아니라
  2024-12-12 아시아나항공 지분 [63.88%] 인수(영업권 [1,399,712백만원])의 **첫 온기 연결**이라 비용이
  [15.76→24.11조]로 매출 증가분을 넘겼다. **리스부채 [13.04조]는 전용 캡션이 있는데 사용권자산은 유형자산
  [32.94조] 안에 열로 숨음**(이마트 V-107③의 거울상) · **마일리지 이연수익 [3.78조]**가 운전자본을 [+0.27조]
  풀어 줌(신규 카드 `n25`) · 임차기정비충당부채전입 [0.50조] · 단일 CIS · 중단영업 없음.
  `--strict` 첫 조립 후 갭 1건(즉시 해소) · `--all` 17본 0 · accuracy 14→0 · **BS 계정셀 57/57** ·
  나브 접힘 0@1440·1280·1024 · pytest 130 + 10/10.
- **08-05 V-110 후속 정비(전 골든)**: `check_golden`이 `strings`를 아예 안 보고 있어 **4본이 개요·에필로그·인트로
  전량 공란인 채 서빙**되고 있었고, `knots[].story`도 7본 113개가 공란이었다. **§16 신설 + 12본 백필 + 렌더러
  `intro_lead` 배선**으로 해소(커밋 `e55048aa`). 교훈: **`--strict` 0은 "게이트가 보는 범위에서 0"일 뿐**이다.

## ✅ 리더 판단 대기 — **없음**

V-109 (C)를 "리더 판단 대기"로 넘겼던 것은 **오판이었다**(리더 지적으로 재조사 → V-110으로 전량 처리).
같은 실수를 반복하지 말 것: **발견하면 채록하고 고치는 데까지 간다.** 대기열에 넣는 건 ⓐ진짜로 갈림길이라
선택이 필요할 때(작업 *순서* 등) ⓑ리더만 아는 사업 판단이 필요할 때뿐이다.

---

## ▶ 다음 본 — 크래프톤 259960 (6/8), S0부터 시작

### S0 프리플라이트 실측 (08-05 확인 — 재확인만 하고 바로 S1로)

```
sectioning health: OK ✅
series 15/24 · 미완성 9: revenue · cogs · gross · sgna · div · buyback · dep · rnd · dsOp
rcept_no 20260515002689 · 주석 33개(주1~33, 하위번호 13-1·13-2·20-1·20-2 포함, 결번 0)
fs_account FY2025: BS 45 · IS 0 · CIS 26 · CF 51 · SCE 119
```

- ⚠️ **`revenue` 미완성은 계정명 변이다** — fs_account에 **`영업수익`**으로 5개년 실존:
  `[1.886, 1.854, 1.911, 2.710, 3.327]조`. `series.SOURCE_MAP['revenue']`가 `ifrs-full_Revenue`만 보고 있다.
  **먼저 `--all` 영향을 실측한 뒤** `ALT_NAME['revenue'] = r"^영업수익$"` 추가가 맞는지 판단할 것
  (V-109⑦ 선례: 정규식만 넓히면 다른 티커에서 기준이 섞일 수 있으니 **before/after 전 티커 diff 필수**).
- ⚠️ **cogs·gross·sgna 부재 = lump-opex(플랫폼형)** — 주23 `영업비용`(892자)이 근거다.
  V-010(NAVER)·V-077(SKT) 동형이니 **`is-opex` + 성격별 서브행, k3·k4 생략**(R6.9 방향A). 구조 패턴표 참조.
- ⚠️ **단일 CIS**(IS 0행, V-011 동형) — `sj_div` 가정 금지.
- ⚠️ **주12 관계기업·공동기업투자가 111,200자 괴물블록**, 주1 일반적인 사항도 62,397자다.
  health는 OK이고 주1~33 결번 0이라 **한전 주16(V-102 선례)처럼 실제 거대 주석일 가능성이 높지만**,
  S1 착수 전에 별도FS 유입이 아닌지 눈으로 한 번 확인할 것.
- ⚠️ **하위번호 주석 4개**(13-1·13-2·20-1·20-2) — 원장 라우팅·`note_dive` 키를 **문자열 그대로**(`"13-1"`) 쓸 것(V-002 계열).
- ⚠️ **FY2025 보고서가 5월 제출**(`20260515002689`)이다. 다른 골든은 3월이라 습관으로 3월 rcept를 쓰지 말 것.
- **미완성 키 처리**: `div`·`buyback`·`dep`·`rnd`·`dsOp`는 근거 없으면 `five=skip`(R6.9 방향A).

### 구조 가설 (S1에서 실주석으로 **확정** — 가설은 가설일 뿐)

- **주21 주식기준보상(24,824자)이 게임사 시그니처** — 개발 인력 보상이 영업비용의 큰 축일 수 있다.
  주23 영업비용 성격별 구성과 반드시 대조할 것.
- **주14 무형자산(10,473자)·주32 사업결합** — 게임 IP·스튜디오 인수가 있으면 그해 서사의 축이 될 수 있다.
  ⚠️ 금칙어 "매수" 금지(→인수) · **합계 라인을 특정 거래 금액으로 쓰지 말 것**(체크리스트 26항).
- **주12(111,200자)** — 관계·공동기업 투자 목록이 거대하다. 지분 투자로 스튜디오를 넓히는 구조라면
  `n12`가 이 본의 핵심 카드가 된다.
- **영업수익 [1.9조]대에서 [3.33조]로 2년 만에 급증** — 무엇이 그 증가를 만들었는지(신작·기존작·플랫폼)가
  서사 축 후보다. 주22(고객과의 계약)·주30(영업부문)에서 확정하라.
- **주19 우발채무·약정 17,055자**·**주26 법인세 14,407자**·**주29 특수관계자 16,428자**도 크다.

---

## 실행

**[WAVE2_BATCH_PROMPT.md](WAVE2_BATCH_PROMPT.md)를 읽고 그대로 실행한다.** 진행 위치는 `git log --oneline -5`와
`corps.csv` tier(1=완주·1c=대기)로 파악.

### 이번 세션에 특히 달라진 점 (V-109·V-110 반영)

1. **패널 C(현금흐름) 세부 행은 `fs_account`가 아니라 원문 CF 표에서 읽어라**(V-109⑥) —
   fs_account는 취득·상환을 양수로 저장해 **그룹 합이 총계와 안 맞는다**. 소스는
   `integration/dossier/data/report_<t>.json`의 `statements.cf`. 조립 전에
   `python integration/dossier/build_report_source.py <t>`로 먼저 만들어 두면 편하다.
2. **`strings`(intro_lines 2줄·overview·epilogue)와 `knots[].story`는 이제 `--strict` §16이 강제한다**(V-110).
   조립 단계에서 반드시 채울 것 — `knots.story`는 **해당 dive의 `what[0]` 동기화**가 계약이다.
3. **`--strict`에 §14·§15·§16이 있다.** §14 anchor 자기정합 · §15 Zone C/E 헤드라인 라벨 ≤22자 ·
   §16 strings·knots.story 공란 0.
4. **S1 직후 `python -m modules.report.facts_lint 259960` — ERROR 0 확인이 의무.**
   note-extractor 호출 프롬프트에 **항목 스키마를 예시와 함께 명시**할 것
   (`{name, value, value_key, unit, period, source_quote, src}` + 다열 행은 `cols{}`).
   대한항공 실측: 12배치 · 46/46 주석 · **1,414항목 · ERROR 0 · WARN 0**.
5. **완주 보고에 `python -m modules.report.check_golden 259960 --links` 수치를 넣는다**(대한항공 57/57 선례).

### 완주 정의

```bash
python -m modules.report.facts_lint 259960                      # ERROR 0
python -m modules.report.check_golden 259960 --strict           # §1~§16 갭 0(무핀 0 포함)
python -m modules.report.check_golden 259960 --links            # 계정셀 링크 실측 → 보고
python -m modules.report.check_golden --all --strict            # 전 골든 무회귀(현재 17본 0)
GALAXY_TICKER=259960 python -m pytest tests/report/test_galaxy_interaction.py
python -m pytest tests/report/ -q                               # 현재 130 passed, 1 skipped
```
\+ **accuracy-verifier REFUTED 0** · **completeness 삼성 T0 패리티** · **라이브 렌더 스윕**(클릭 사이 **Esc** 필수 ·
APPENDIX 나브 접힘 0@1440·1280·1024 · 1024px 가로 오버플로 0 · 콘솔 0)

완주 후: VARIATIONS 정본 §4에 **V-111부터** 채록(증상→원인→**게이트가 왜 못 잡았나**→처리) + 채록 로그 1줄 ·
`corps.csv` 1c→1 · `build_report_source.py 259960` · `build_galaxy_index.py` · **1커밋 push**(PR 금지).
승인 대기 없이 다음 본(하이브 352820)으로.

---

## 보고 규약 (컨텍스트 절약 — 리더 지시 2026-08-03)

- **착수** 1줄: `[티커/회사] S0: health OK · series N/24 · 주석 M개`
- **단계 전환** 1줄: `S1 fact N건 / S2 dive N장 / S3 산문 N카드`
- **게이트**: 숫자만 — `strict 0 · --all 18본 0 · accuracy REFUTED n건(정정) · links N/N · pytest 130 + 10/10`
- **완주**: 5줄 이내 + localhost URL
- **상세 보고는** ⓐ게이트 3회 미수렴 ⓑ리더 판단 필요한 구조 결정 ⓒ데이터 결함 ⓓ코드 승격 후보(2회+) **일 때만**
- **금지**: 파일 내용·JSON·카드 목록 덤프 · 카드별 산문 나열 · 진행 과정 서술 · 이미 문서에 있는 규칙 재설명

## 주의

- 인코딩 `PYTHONUTF8=1`. 로컬 서버 `python -m http.server 8000`.
- `reports.db`(shared/data/)·`facts/`·`raw_cache/`는 gitignore — `--strict` §7·§10·§13·`--links`·`facts_lint`는
  **이 머신에서만** 유효(CI skip).
- **main 직접 push·force push 금지.** 커밋은 `feat/golden-wave2-industries`에만.
- 서브에이전트는 **self-contained 프롬프트**(대화 맥락을 못 봄) — fact 발췌·series·카드 키·견본 원문을
  프롬프트에 직접 넣을 것. 컨텍스트 절약을 위해 **에이전트가 파일로 쓰게 하고 오케스트레이터는 경로만 받는** 방식이
  유효했다(대한항공에서 산문 11배치·감사 6배치를 이 방식으로 처리).
- ⚠️ **세션 한도**로 서브에이전트가 중도 실패할 수 있다 — 실패하면 그 단계를 **직접** 수행하거나 재실행할 것
  (대한항공 S5가 이렇게 죽어 정적 감사는 스크립트로, 라이브 스윕은 오케스트레이터가 직접 했다).

## 잔여 리더 트랙 (이 브랜치 밖)

1. **T2 클러스터 확장** — 기존 12 + 신규 8클러스터의 나머지 기업(각 T1 견본 캐스케이드, §8.5). 별도 브랜치.
2. dead-click 2~3행 렌더러 버그(V-059) · 900px 나브 접힘(V-103) — 전 골든 동형·콘텐츠 무관, 렌더러 트랙.
3. **`strings.header`·`hero` 중복 저작 정리**(V-110 후단) — 템플릿이 `corp.*`로 같은 내용을 이미 그려 미사용이다.
   스키마에서 뺄지 여부만 남았다(비워 둬도 화면 손실 없음).
4. **`report_<t>.json` 완전성 계측**(V-109 사각 A) — 본표 절단 안내 문단 검출을 `--links`에 넣는 안.
