# 다음 세션 프롬프트 — Wave 2 신규 산업 T1 골든 8본 (브랜치 `feat/golden-wave2-industries`)

> 이 파일은 다음 세션에 붙여넣을 핸드오프다. 갱신: 2026-08-05.

## 현재 상태

- **골든 20본 목표 중 18본 완주**: 삼성 005930(T0) + T1 17사. 전원 `--strict` §1~§16 갭 0.
- **Wave 2 진행 6/8 완주** — 철강 현대제철 004020(V-102) · 정유 S-Oil 010950(V-104) · 식품 CJ제일제당 097950(V-105) ·
  유통 이마트 139480(V-107) · 항공 대한항공 003490(V-109) · **게임 크래프톤 259960(V-111, 08-05)**.
  남은 2본: **엔터 하이브 352820** · 유틸리티 한국전력 015760.
  **PR 없이 본당 1커밋 push만**(리더 지시). T2 확장은 이 브랜치 범위 밖.
- **크래프톤 259960 완주 요약**: 매출 [3.33조]는 5년 새 최대인데 영업비용이 [1.53→2.27조]로 더 크게 늘어
  영업이익 [1.18→1.05조], 순이익은 [1.30→0.73조]. 진짜 자리는 영업이 아니라 **영업외**([+0.54조]→[-0.10조]) —
  작년을 밀어 올린 FVPL 평가·처분이익이 빠지고 무형자산손상 [763억원]·관계기업손상 [352억원]이 붙었다.
  배경은 **5개 스튜디오 인수**(ADKRAFTON 이전대가 [7,080억원]·영업권 [4,482억원] 잠정)로 무형자산 [0.66→1.80조].
  **사용권자산도 리스부채도 재무상태표에 전용 캡션이 없어**(은닉 제3형) `n13-2`를 흐름 행 `cf-fin-lease`에 앵커.
  배당 0·자기주식 [0.33조]인데 **잔액 증가의 절반 [1,573억원]이 넵튠이 들고 온 크래프톤 주식**.
  `--strict` **첫 조립 0** · `--all` 18본 0 · accuracy 18→0 · **BS 계정셀 35/35** · TOC 착지 35/35 ·
  나브 접힘 0@1440·1280·1024 · pytest **132** + 인터랙션 **9 passed/1 skipped**.

## ✅ 리더 판단 대기 — **없음**

발견하면 채록하고 **고치는 데까지 간다**(V-110 교훈). 대기열에 넣는 건 ⓐ진짜 갈림길(작업 *순서* 등)
ⓑ리더만 아는 사업 판단일 때뿐이다.

## ⚠️ 이번 세션에 난 사고 — 반드시 예방할 것

**completeness-auditor 서브에이전트가 저장소 루트에 `rm -f`를 실행해 작업과 무관한 `aws-signin.png`(untracked)을
삭제**했다. git 복구 불가(Git Bash `rm`은 휴지통 미경유). S1 배치에서도 `scratch_check.json`이 저장소 루트에 남았다.
→ **모든 서브에이전트 호출 프롬프트에 아래 한 줄을 넣을 것**:

> ⚠️ 저장소 안의 파일을 **삭제·이동하지 말 것**. 임시 파일은 지정된 스크래치 경로에만 만들고, 정리도 하지 말 것.

2회째면 `.claude/agents/*.md` 계약에 승격한다.

---

## ▶ 다음 본 — 하이브 352820 (7/8), S0부터 시작

### S0 프리플라이트 실측 (08-05 확인 — 재확인만 하고 바로 S1로)

```
sectioning health: OK ✅
series 19/24 · 미완성 5: div · buyback · dep · rnd · dsOp
rcept_no 20260320000802 (2026-03-20 제출) · 주석 36개(주1~36 연속, 하위번호 없음, 결번 0)
fs_account FY2025: BS 51 · IS 0 · CIS 28 · CF 42 · SCE 144
```

- ⚠️ **단일 CIS**(IS 0행, V-011 동형 — Wave2에서 004020·010950·097950·003490·259960에 이어 6번째). `sj_div` 가정 금지.
- ✅ **cogs형이다** — series에 `cogs`·`gross`·`sgna`가 완결돼 있고 주23 `판매비와관리비`(1,025자)·
  주24 `비용의 성격별 분류`(931자)가 **둘 다** 있다. 크래프톤·NAVER 같은 lump-opex가 **아니다**.
  ⚠️ **주24 성격별 합계는 매출원가+판관비 합계**다(V-102 교훈) — 매출원가 카드에 그대로 붙이면 어긋난다.
- ⚠️ **주10 공동기업 및 관계기업 투자가 47,427자·주1 연결회사의 개요가 42,417자** — 크래프톤 주12(111k자)
  선례처럼 실제 거대 주석일 가능성이 높지만, S1 착수 전 별도FS 유입이 아닌지 눈으로 한 번 확인할 것.
- ⚠️ **주21 주식기준보상 26,153자** — 엔터·플랫폼 공통 시그니처. 다만 크래프톤에서 "주식보상이 비용의 큰 축"이라는
  가설이 **실주석에서 반증**됐으니(V-111①) 가설로 서사를 정하지 말고 주24 성격별 구성과 반드시 대조하라.
- ⚠️ **주6 리스(3,799자)** — 하이브도 사용권자산·리스부채가 재무상태표에 전용 캡션으로 있는지 **fs_account BS
  계정명으로 먼저 확인**하라. 없으면 V-111③(양쪽 은닉) 처방을, 한쪽만 없으면 V-107③/V-109② 처방을 쓴다.
- ⚠️ **주35 사업결합(965자)·주36 보고기간 후 사건(1,293자)** — 하이브는 레이블 인수 이력이 잦다. 있으면
  `합계 라인을 특정 거래 금액으로 쓰지 말 것`(체크리스트 26항)을 특히 조심.
- **미완성 키 처리**: `div`·`buyback`·`dep`·`rnd`·`dsOp`는 근거 없으면 `five=skip`(R6.9 방향A).
  단 `rnd`는 「II.사업의내용」에서 5개년 회수가 가능할 수 있다(V-102⑦·V-111⑦ 2회 성공 —
  원문 표가 매년 직전 3개년을 겹쳐 실으므로 **rcept만 바꿔 과거 보고서를 조회**하면 된다:
  FY24=20250321001187 · FY23=20240322001002 · FY22=20230403004275 · FY21=20220322000653).

### 구조 가설 (S1에서 실주석으로 **확정** — 가설은 가설일 뿐)

- **주34 영업부문 정보(3,805자)** — 레이블/플랫폼/기타 부문 구조가 서사 축 후보.
- **주12 무형자산(5,644자)** — IP·음원·전속계약 자산화 여부. ⚠️ **영업권은 상각 대상이 아니다**(손상만).
- **주10(47,427자)** — 공동기업·관계기업이 거대하면 `n10`이 이 본의 핵심 카드가 된다.
- **주15 차입금 및 전환사채(3,123자)** — 전환사채가 있으면 희석 EPS·자본 서사와 엮인다.
- **주9 재고자산(813자)** — 앨범·MD 재고. 크래프톤엔 없던 계정이라 §10 BS앵커가 발화한다.

---

## 실행

**[WAVE2_BATCH_PROMPT.md](WAVE2_BATCH_PROMPT.md)를 읽고 그대로 실행한다.** 진행 위치는 `git log --oneline -5`와
`corps.csv` tier(1=완주·1c=대기)로 파악.

### 이번 세션에 특히 달라진 점 (V-111 반영)

1. **`pbs-cash`(Zone A)는 기초현금이다** — 골든 15본 전수 `pbs-cash == cf-begincash`. 당기말을 넣으면
   표시 반올림이 같아 **`--strict` 16개 절이 전부 통과**한다(V-111 🔍A). 조립 직후 이 등식을 눈으로 확인할 것.
2. **`five.valley`는 `anchor.label`을 그대로 찍는다** — `valley`를 anchor 연도가 아닌 곳에 두면 그 지점에
   엉뚱한 사건 라벨이 붙는다. §14는 `anchor.shared_keys`에 든 키만 보므로 **나머지는 체커가 안 잡는다**(V-111 🔍B).
   원칙: **`valley`는 `anchor.valley_index`와 같게 두거나 아예 생략**한다.
3. **인터랙션 테스트의 `skip`을 완주 보고에 그대로 적을 것** — `9 passed/1 skipped`처럼. skip은 FAIL이 아니라
   조용히 커버리지를 깎는다(전 골든 공통 JOURNEY 데드존, 삼성 T0 포함 — 콘텐츠 무관).
4. **패널 C 세부는 원문 CF 표에서**(V-109⑥) · **`strings`·`knots[].story`는 §16이 강제**(V-110) — 종전대로.
5. **S1 직후 `python -m modules.report.facts_lint 352820` — ERROR 0 확인이 의무.**
   note-extractor 호출 프롬프트에 **항목 스키마를 예시와 함께 명시**할 것
   (`{name, value, value_key, unit, period, source_quote, src}` + 다열 행은 `cols{}`).
   크래프톤 실측: 12배치 · 36주석 · **1,226항목 · ERROR 0 · WARN 0**.
6. **완주 보고에 `python -m modules.report.check_golden 352820 --links` 수치를 넣는다**(크래프톤 35/35 선례).

### 완주 정의

```bash
python -m modules.report.facts_lint 352820                      # ERROR 0
python -m modules.report.check_golden 352820 --strict           # §1~§16 갭 0(무핀 0 포함)
python -m modules.report.check_golden 352820 --links            # 계정셀 링크 실측 → 보고
python -m modules.report.check_golden --all --strict            # 전 골든 무회귀(현재 18본 0)
GALAXY_TICKER=352820 python -m pytest tests/report/test_galaxy_interaction.py
python -m pytest tests/report/ -q                               # 현재 132 passed, 1 skipped
```
\+ **accuracy-verifier REFUTED 0** · **completeness 삼성 T0 패리티** · **라이브 렌더 스윕**(클릭 사이 **Esc** 필수 ·
APPENDIX 나브 접힘 0@1440·1280·1024 · 1024px 가로 오버플로 0 · 콘솔 0 · **원문 TOC 전 주석 착지 실패 0**)

완주 후: VARIATIONS 정본 §4에 **V-112부터** 채록(증상→원인→**게이트가 왜 못 잡았나**→처리) + 채록 로그 1줄 ·
`corps.csv` 1c→1 · `build_report_source.py 352820` · `build_galaxy_index.py` · **1커밋 push**(PR 금지).
승인 대기 없이 다음 본(한국전력 015760)으로.

### 남은 코드 승격 후보 (V-111 🔍 — 다음 세션에서 `--all` 실측 후 처리)

- **(A) `--strict`에 `pbs-cash.raw_mn == cf-begincash.raw_mn` 추가** — 저비용·결정론. 전 골든 실측상
  비교 가능한 15본이 이미 만족하므로 무회귀일 가능성이 높다.
- **(B) `valley` 정합 확대** — `valley` 지정된 전 dive를 `anchor.valley_index`와 대조. ⚠️ 기존 골든에
  정당한 불일치가 있는지(NAVER k8 `valley=1` vs anchor 0 등) `--all` 실측이 선행돼야 한다.
  대안: 렌더러에 dive별 `valleyLabel`을 도입해 라벨을 카드가 정하게 하는 방법(렌더러 트랙).

---

## 보고 규약 (컨텍스트 절약 — 리더 지시 2026-08-03)

- **착수** 1줄: `[티커/회사] S0: health OK · series N/24 · 주석 M개`
- **단계 전환** 1줄: `S1 fact N건 / S2 dive N장 / S3 산문 N카드`
- **게이트**: 숫자만 — `strict 0 · --all 19본 0 · accuracy REFUTED n건(정정) · links N/N · pytest 132 + 인터랙션 N/M`
- **완주**: 5줄 이내 + localhost URL
- **상세 보고는** ⓐ게이트 3회 미수렴 ⓑ리더 판단 필요한 구조 결정 ⓒ데이터 결함 ⓓ코드 승격 후보(2회+) **일 때만**
- **금지**: 파일 내용·JSON·카드 목록 덤프 · 카드별 산문 나열 · 진행 과정 서술 · 이미 문서에 있는 규칙 재설명

## 주의

- 인코딩 `PYTHONUTF8=1`. 로컬 서버 `python -m http.server 8000`.
- `reports.db`(shared/data/)·`facts/`·`raw_cache/`는 gitignore — `--strict` §7·§10·§13·`--links`·`facts_lint`는
  **이 머신에서만** 유효(CI skip).
- **main 직접 push·force push 금지.** 커밋은 `feat/golden-wave2-industries`에만.
- 서브에이전트는 **self-contained 프롬프트**(대화 맥락을 못 봄) — fact 발췌·series·카드 키·견본 원문을
  프롬프트에 직접 넣을 것. **브리프 JSON을 파일로 만들어 경로만 넘기고, 산출물도 파일로 쓰게 하는 방식**이
  컨텍스트 절약에 유효했다(크래프톤에서 S1 12배치·S3 11배치·S4 9배치를 전부 이 방식으로 처리).
- ⚠️ **세션 한도**로 서브에이전트가 중도 실패할 수 있다 — 실패하면 그 단계를 **직접** 수행하거나 재실행할 것.
- ⚠️ **서브에이전트에게 저장소 파일 삭제·이동을 금지하는 문구를 반드시 넣을 것**(위 사고 참조).

## 잔여 리더 트랙 (이 브랜치 밖)

1. **T2 클러스터 확장** — 기존 12 + 신규 8클러스터의 나머지 기업(각 T1 견본 캐스케이드, §8.5). 별도 브랜치.
2. dead-click 2~3행 렌더러 버그(V-059) · 900px 나브 접힘(V-103) — 전 골든 동형·콘텐츠 무관, 렌더러 트랙.
   **V-111 완전성 감사가 근본 원인을 좁혔다**: 그룹헤더 행(`bs-liab` 등)에서 펼침 캐럿이 라벨 텍스트와 같은
   flex 블록에 있어 **라벨이 길수록 캐럿이 행 중앙으로 밀려** 정중앙 클릭을 가로챈다(`onToggle`의
   `stopPropagation`이 핀을 막음). 처방 후보: 캐럿에 `marginLeft:auto`를 줘 값 쪽 끝에 고정.
3. **`strings.header`·`hero` 중복 저작 정리**(V-110 후단) — 템플릿이 `corp.*`로 같은 내용을 이미 그려 미사용이다.
4. **`report_<t>.json` 완전성 계측**(V-109 사각 A) — 본표 절단 안내 문단 검출을 `--links`에 넣는 안.
5. **JOURNEY 하이라이트 데드존**(V-111 🔍C) — 전 골든 공통(삼성 T0 포함, y≈2000~2400 구간에서 `data-jl` 부재).
   인터랙션 테스트가 이 구간을 샘플하면 결정론적으로 skip된다. 렌더러 트랙.
